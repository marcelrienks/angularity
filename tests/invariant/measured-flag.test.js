describe('Measured Flag Correctness', () => {
  it.todo('isInterpolated === false only for measured positions (all 169 cells)');
  it.todo('required positions (-6, -3, 0, +3, +6) never interpolated');
  it.todo('density change updates all flags correctly');
});
